//QCC:
public class groupSum {
	//absolutely no idea at all
	//we dont understand what the base case should be and
	//how we should recurse the program if not all elements are in the sum
	//ex: [2, 4, 8] can sum to 10 but the 4 must be ignored
	return true;
}
